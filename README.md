Project setup for the backend.

npm init -y

npm i -D typescript

tsc --init

Then modify typescript.json (root directory & output directory).
npm i express mongoose zod jsonwebtoken cors dotenv (These are the main dependencies).

npm i -D ts-node-dev @types/ express @types/cors @types/dotenv @types/jsonwebtoken

Setting Up ES Lint

for test branch



